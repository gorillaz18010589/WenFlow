package com.wen.flow.repositories;

import android.app.Application;

import androidx.annotation.NonNull;
import androidx.lifecycle.AndroidViewModel;

public class ImageRepository extends AndroidViewModel {
    
    public ImageRepository(@NonNull Application application) {
        super(application);
    }
}
